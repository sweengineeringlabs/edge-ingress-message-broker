//! SAF consumer facade — message consumer module.
//!
//! This module provides the `saf/consumer` layer required by the
//! `saf_consumer_int_test.rs` integration test, which validates that the
//! `TransportSvc` factory creates healthy consumers.
//!
//! Concrete types and traits are exported from `lib.rs` directly.
